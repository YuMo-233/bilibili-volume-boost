# B 站音量增强插件（Bili Volume Boost）

一个 Chrome/Edge Manifest V3 浏览器插件：为 bilibili.com 的视频与直播提供超过原生 100% 上限的音量增益，按 UP 主记忆音量，且全程不破坏 B 站原生的沉浸全屏。领域语言在此建档，代码与文档必须使用本文件的规范术语。

## Language

### 全屏三态

**沉浸全屏**:
B 站播放器通过 Fullscreen API（`requestFullscreen`）进入的原生系统全屏，视频占满整个屏幕、页面元素全部隐藏。这是插件唯一必须保护的全屏形态，任何情况下不得让其失效或降级。
_Avoid_: 普通全屏、原生全屏

**网页全屏**:
B 站播放器的"网页全屏"按钮，视频铺满页面但仍受浏览器标签页、系统导航栏遮挡，未调用 Fullscreen API。
_Avoid_: 伪全屏、页面全屏

**浏览器全屏**:
按键盘 F11 触发的浏览器窗口级全屏，页面布局不变、地址栏隐藏。
_Avoid_: 全屏、系统全屏

### 音量领域

**原生音量**:
B 站播放器自带的 0%–100% 音量控制，插件不修改它，代码对其零改动。
_Avoid_: 基础音量

**增益（Gain）**:
插件新增的独立音量轨道，采用**感知等量刻度**（Loudness）范围 50%–500%（对应幅值 0.315–14.62x，Stevens 幂律，见 docs/adr/0004），叠加在原生音量之上。原生音量管 0–100%，增益管 100% 以上为主，50–100% 用于压低过响素材，二者分层互不干扰。
_Avoid_: 增强音量、boost 层

**总响度**:
播放器实际输出的音量，等于「原生音量 × 增益」。启用插件（总开关打开）即接入音频管线，与增益是否偏离 100% 无关。

**削波（Clipping）**:
增益过高时波形峰值超过满刻度被硬切造成的爆音/噼啪失真。插件通过压缩限幅防止。

**压缩限幅（Limiting Compression）**:
串联在增益节点后的 DynamicsCompressorNode，阈值约 -1dB、比例 20:1、极短 attack，平时透明、仅在峰值逼近满刻度时压低，用于防止削波爆音。
_Avoid_: 保护音质、无损

**极限区**:
感知值 300% 以上的区段（含 300% 以上的刻度点），只是"这里可能已经在限幅"的**视觉提示**，不承诺任何音质。UI 表现为刻度常亮琥珀、填充与滑块描边转琥珀、数值转琥珀（见 docs/adr/0004）。
_Avoid_: 无损区、安全区、红线区

**限幅读数**:
面板上显示的实时限幅量（形如「限幅-3dB」），直接取自 `DynamicsCompressorNode.reduction`，代表压缩机此刻压掉了多少 dB，0 表示未介入。它是"是否失真"的唯一事实来源，取代按档位推断。低于 1dB 不显示；取最近 1 秒内的峰值以避免逐帧跳动。
_Avoid_: 失真度、压缩比

### 记忆与存储

**UP 主记忆**:
以 UP 主 mid（视频页）或主播 uid（直播页）为键保存的增益值，进入对应页面时自动应用。
_Avoid_: 用户偏好、设置保存

**记忆分桶**:
同一 UP 主的视频与直播各占一条记忆记录（分桶存储），互不影响，因为直播普遍需要更高增益。
_Avoid_: 子记录、维度

**LRU 淘汰**:
记忆配额按类型独立：视频记忆上限 1024 条、直播记忆上限 256 条，各自超出时淘汰该类型 lastUsed 最旧的记录。
_Avoid_: 清理、清理策略

### 播放器

**播放器适配**:
视频页播放器与直播播放器 DOM 结构不同，需分别适配（选择器、控制栏注入点、全屏结构）。
_Avoid_: 兼容层、hack

**控制栏（Player Control Bar）**:
B 站播放器底部的控制条，插件的悬浮滑块注入点（原生音量按钮旁）。
_Avoid_: 工具条、底部栏

**锚点（Anchor）**:
控制栏中的**原生音量按钮**，悬浮滑块插入其右侧。锚点存在且可见，是「控制栏已渲染」的客观信号（见 docs/adr/0006）。
_Avoid_: 参照物、定位点

**控件就绪（Control Readiness）**:
控制栏已渲染稳定、可安全显示插件 UI 的状态。判定为：锚点存在 + 锚点可见（rect 宽高均 > 0）+ 锚点位置连续两次采样一致。就绪是**一次性闩锁**，放行后不因后续重排重新隐藏。
_Avoid_: 加载完成、就绪

**几何稳态**:
浮层可以显形的判据：几何签名（left/top/w/h）连续 300ms 未再变化，即认为宿主布局已定型。未定型期间整个 slot `visibility: hidden`，避免把元素画在"当下"的错误位置、之后又突然回正（见 docs/adr/0009）。
_Avoid_: 加载完成、渲染完毕

**兜底放行**:
锚点 5 秒内始终未出现时，退回「追加到控制栏末尾」并放行的降级路径，用于兼容匹配不到锚点的播放器变体。
_Avoid_: 降级、超时

**悬停残留（Hover Residue）**:
宿主元素在鼠标静止时被移动，浏览器**不补发 `mouseleave`**，导致 `.bv-open` 长期残留、面板无法自动收起的故障态。宿主每次移动后必须主动清除（见 docs/adr/0006）。
_Avoid_: 卡住、悬停状态

**音频源挂载（Source Attachment）**:
将播放器 video 的音频接入插件 Web Audio 管线（`captureStream()` 抽头 → `MediaStreamAudioSourceNode`）的过程。B 站切换清晰度/切流会重建 video 元素，需要监听并自动重挂，避免多路音频叠加（见 docs/adr/0010）。
_Avoid_: 绑定、连接

**捕获抽头（Capture Tap）**:
`HTMLMediaElement.captureStream()` 产出的一条**只读**音频轨：它是解码后原始音频，既不占用元素的音频槽位、也不受元素 `volume`/`muted` 影响。因不占槽位，B 站自建的 `MediaElementAudioSourceNode` 可正常创建，从而规避页内换集卡死（见 docs/adr/0010）；代价是原生音量层需由插件镜像（见 docs/adr/0002）。
_Avoid_: 录制、监听

**抽头可发声判定（Tap Audibility Gate）**:
抽头路径下「原元素可否被静音」的唯一判据：仅当「音频上下文 running **且** MAIN world 静音钩子已就绪 **且** 抽头音轨确有声（`readyState==='live'` 且未 `muted`）」三者同时成立，才静音原元素并放行本图增益；否则原元素保持原生发声、本图增益归零（降级为"只有原生声音、无增益"）。抽头是静音原元素后的唯一声源，抽头不可用仍静音原元素即整页无声（见 docs/adr/0011）。
_Avoid_: 接管判定、就绪判定

**重抽（Recapture）**:
抽头音轨在媒体源变化（换源 / 换集 / 切清晰度）后 `ended` 时，用同一 video 重新取一条 `captureStream()` 抽头接回原增益链的过程；取不到则先恢复原生发声再退避重试，杜绝无声窗口（见 docs/adr/0011）。
_Avoid_: 重挂、重新捕获

**独占槽位（Exclusive Slot）**:
一个 `HTMLMediaElement` 至多对应一个 `MediaElementAudioSourceNode`，且**创建后不可释放**——重复创建抛 `InvalidStateError: HTMLMediaElement already connected previously`。插件若抢占它，B 站后续自建音频图会失败并卡死播放，这是本插件已弃用的机制（见 docs/adr/0010）。
_Avoid_: 音频通道、端口

**重挂（Reattachment）**:
video 元素被 B 站重建后，断开旧音频源并重新挂载新 video 的过程。

### 醒目留言浮层

**醒目留言（SC）**:
B 站直播的付费置顶留言，页面原生组件内部名为 `pay-note`，原生展示位于右侧栏、沉浸全屏时不可见（见 docs/adr/0005）。属于**长悬挂内容**（最短 60 秒、最长 2 小时），与转瞬即逝的弹幕有本质区别。
_Avoid_: 超级留言、付费弹幕

**轮询取数**:
SC 的唯一取数路径：以固定间隔（10 秒）请求 `GET /av/v1/SuperChat/getMessageList?room_id=`（无需登录），返回当前悬挂中的全部 SC 及精确配色字段。以 `id` 为主键做增量合并，接口中消失的 `id` 判为已移除。因 SC 长悬挂，轮询不会漏项。**room_id 必须是真实房间号**，短号会被接口拒绝。
_Avoid_: 定时请求、轮询同步

**真实房间号 / 短号**:
直播 URL 首段数字可能是**短号**（如 `/88888`），直播接口只认**真实房间号**（如 `3947892`）；用短号请求会返回 `code:-1「系统繁忙」`，表现为"该房间取不到任何数据"。取数前必须经 `room_init` 换算（见 docs/adr/0005）。
_Avoid_: 房间 id、房间标识

**悬挂**:
一条 SC 从 `start_time` 到 `end_time` 的展示期，期间该留言应常驻可见。
_Avoid_: 展示中、挂载

**留言浮层（Overlay）**:
插件在沉浸全屏下注入播放器列（`.live-player-mounter`）内的 SC 卡片容器，四角定位、默认右上。注入点在播放器内，故跟随全屏。
_Avoid_: SC 面板、弹窗

**SC 卡片**:
浮层内单条留言的渲染单元，含头像、用户名、价格、留言四要素；配色取自协议下发的色值字段（`background_color` / `font_color` 等），不硬编码价格色阶。
_Avoid_: 气泡、通告

**回填（Backfill）**:
进入直播间或开启 SC 开关时**立即执行的首轮轮询**，一次性带回当前全部悬挂中的 SC，补齐进入之前就已存在、不会再产生"新增"事件的留言。此后转入固定间隔轮询维持增量。
_Avoid_: 补拉、同步

**折叠**:
同时悬挂的 SC 超过叠放上限（3 条）时，超出部分汇总为 "+N 条"，价格高者优先展示。
_Avoid_: 收起、省略

**SC 开关**:
popup 中独立于音量总开关的 SC 浮层开关，默认开启。
_Avoid_: SC 设置

**显示位置**:
SC 浮层的四角位置配置（左上 / 右上 / 右下 / 左下），默认右上；仅作用于浮层，音量滑块仍跟随原生控制栏。
_Avoid_: 卡片位置

### 详情页相关推荐

**相关推荐列表**:
视频详情页右侧栏的推荐视频列（点击「换一换」会整列重排），由 `x/web-interface/archive/related` 渲染，每张卡片带 bvid/aid/UP 主链接，但不含「不感兴趣」理由清单。
_Avoid_: 侧推荐、相关视频

**⋮ 菜单**:
原生视频卡片「更多操作」的三点按钮，点开含「内容不感兴趣」和「不想看此UP主」两项。插件在相关推荐卡片上注入同款入口，位置与原生一致（**贴信息区右上角、与标题第一行对齐**，18×18，标题预留 30px 右侧内边距），**默认隐藏、鼠标移到卡片上才显形**，悬停入口弹出菜单。
_Avoid_: 更多按钮、三点按钮

**不感兴趣上报**:
通过 B 站原生接口上报（`x/web-interface/feedback/dislike`，含 aid、UP 主 mid、csrf），使推荐系统真实降低此类内容权重，是"内联推荐系统"而非本地假移除（见 docs/adr/0008）。实测该接口不校验 WBI 签名。菜单两项直达，无二级理由菜单。
_Avoid_: 点踩、匿名上报

**上报撤销**:
上报后调用 `/dislike/cancel` 撤回报，请求体与上报一致。卡片原位覆盖撤销浮层供用户反悔。
_Avoid_: 取消、回滚

**已反馈态**:
上报后卡片的形态：封面磨砂 + 浮层只盖封面（底色 `rgba(24,26,27,.4)`、圆角 6px）+ 标题信息区隐藏。卡片**不离开列表**，撤销即恢复原状，列表不产生跳动。
_Avoid_: 卡片移除、淡出

**封面磨砂**:
已反馈态下封面图的 `filter: blur(20px)`（带 .3s 过渡），原生用以拉开与浮层文字的对比，视觉上即"磨砂玻璃"效果。
_Avoid_: 模糊、glass

**会话去重白名单**:
本次会话内已上报「不感兴趣」的 bvid 集合，同卡再次出现不注入按钮、不重复上报；内存态，刷新页面即清空。
_Avoid_: 黑名单、记忆

**当前视频开关**:
视频页操作栏「点赞 / 投币」之间的开关式控件，**仅图标**（点赞图标垂直翻转，激活变蓝），悬停提示「不感兴趣」。点一次上报「不想看此UP主」，再点一次撤销。项宽按原生「图标 + 项内留白 26px」定为 50px，使左右视觉间距与原生一致（均 34px）；空间由给投币项加左边距让出，插件不插入原生节点。
_Avoid_: 踩按钮、差评按钮

**项内留白**:
原生操作栏项固定 92px 宽而内容仅约 66px，余下约 26px 是项内空白——相邻项的视觉间距因此约 34px。新增控件必须照搬该留白，间距才能与原有一致。
_Avoid_: padding、空隙

**搜索页卡片**:
搜索结果卡与首页同属竖版 `.bili-video-card` 组件，但**完全不含 UP 主信息**（无 space 链接、无 mid），故上报前需用 `view` 接口补取 UP 主 mid 与 aid。搜索页只跑「不感兴趣」，不接管播放器与音量 UI。
_Avoid_: 搜索项、结果卡

**大封面浮层**:
竖版大封面卡（搜索页 / 首页同款，封面约 217×122）用的原生尺寸浮层（图标 36px、标题 14px、说明 12px、左列 + 右按钮横排）；详情页横版小封面（141×80）则用缩小版。
_Avoid_: 大浮层、完整版

**差评（点踩）**:
对稿件的公开负向评价，与「不感兴趣」是两套体系。**网页端无此接口**，唯一接口为 App 专用并需 App 登录令牌，故本插件不提供（见 docs/adr/0008）。
_Avoid_: 不喜欢、点踩按钮

### 交互

**悬浮滑块**:
注入播放器控制栏的音量增益滑块，Shadow DOM 隔离样式，hover 展开显示百分比，全屏状态下跟随原生控制栏。
_Avoid_: 浮动条、气泡

**组合快捷键**:
插件使用 `Alt+↑/↓` 调增益、`Alt+M` 静音、`Alt+0~9` 定档，全部以 Alt 组合，不触碰 B 站任何原生快捷键。

**防误触**:
焦点位于弹幕输入框、textarea 或可编辑元素时，快捷键全部放行，绝不拦截打字。
_Avoid_: 输入保护