# B 站音量增强插件（Bili Volume Boost）

一个 Chrome/Edge Manifest V3 浏览器插件：为 bilibili.com 的视频与直播提供超过原生 100% 上限的音量增益，按 UP 主记忆音量，且全程不破坏 B 站原生的沉浸全屏。领域语言在此建档，代码与文档必须使用本文件的规范术语。

## Language

### 全屏三态

**沉浸全屏**:
B 站播放器通过 Fullscreen API（`requestFullscreen`）进入的原生系统全屏，视频占满整个屏幕、页面元素全部隐藏。这是插件唯一必须保护的全屏形态，任何情况下不得让其失效或降级。
_Avoid_: 普通全屏、原生全屏

**网页全屏**:
B 站播放器的"网页全屏"按钮，视频铺满页面但仍受浏览器标签页、系统导航栏遮挡，未调用 Fullscreen API。
_Avoid_: 伪全屏、页面全屏

**浏览器全屏**:
按键盘 F11 触发的浏览器窗口级全屏，页面布局不变、地址栏隐藏。
_Avoid_: 全屏、系统全屏

### 音量领域

**原生音量**:
B 站播放器自带的 0%–100% 音量控制，插件不修改它，代码对其零改动。
_Avoid_: 基础音量

**增益（Gain）**:
插件新增的独立音量轨道，范围 100%–500%（对数刻度），叠加在原生音量之上。原生音量管 0–100%，增益管 100% 以上，二者分层互不干扰。
_Avoid_: 增强音量、boost 层

**总响度**:
播放器实际输出的音量，等于「原生音量 × 增益」。只有增益超过 100% 时插件才介入音频管线。

**削波（Clipping）**:
增益过高时波形峰值超过满刻度被硬切造成的爆音/噼啪失真。插件通过压缩限幅防止。

**压缩限幅（Limiting Compression）**:
串联在增益节点后的 DynamicsCompressorNode，阈值约 -1dB、比例 20:1、极短 attack，平时透明、仅在峰值逼近满刻度时压低，用于在极限增益下保全音质。

### 记忆与存储

**UP 主记忆**:
以 UP 主 mid（视频页）或主播 uid（直播页）为键保存的增益值，进入对应页面时自动应用。
_Avoid_: 用户偏好、设置保存

**记忆分桶**:
同一 UP 主的视频与直播各占一条记忆记录（分桶存储），互不影响，因为直播普遍需要更高增益。
_Avoid_: 子记录、维度

**LRU 淘汰**:
记忆配额按类型独立：视频记忆上限 1024 条、直播记忆上限 256 条，各自超出时淘汰该类型 lastUsed 最旧的记录。
_Avoid_: 清理、清理策略

### 播放器

**播放器适配**:
视频页播放器与直播播放器 DOM 结构不同，需分别适配（选择器、控制栏注入点、全屏结构）。
_Avoid_: 兼容层、hack

**控制栏（Player Control Bar）**:
B 站播放器底部的控制条，插件的悬浮滑块注入点（原生音量按钮旁）。
_Avoid_: 工具条、底部栏

**音频源挂载（Source Attachment）**:
将 `MediaElementAudioSourceNode` 连接到播放器 video 元素的过程。B 站切换清晰度/切流会重建 video 元素，需要监听并自动重挂，避免多路音频叠加。
_Avoid_: 绑定、连接

**重挂（Reattachment）**:
video 元素被 B 站重建后，断开旧音频源并重新挂载新 video 的过程。

### 交互

**悬浮滑块**:
注入播放器控制栏的音量增益滑块，Shadow DOM 隔离样式，hover 展开显示百分比，全屏状态下跟随原生控制栏。
_Avoid_: 浮动条、气泡

**组合快捷键**:
插件使用 `Alt+↑/↓` 调增益、`Alt+M` 静音、`Alt+0~9` 定档，全部以 Alt 组合，不触碰 B 站任何原生快捷键。

**防误触**:
焦点位于弹幕输入框、textarea 或可编辑元素时，快捷键全部放行，绝不拦截打字。
_Avoid_: 输入保护